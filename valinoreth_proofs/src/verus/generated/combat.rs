// AUTO-GENERATED by `elicitation generate verus` — DO NOT EDIT
//
// Standalone Verus proof for CombatMachine (V13 assume_specification pattern).
// Regenerate: elicitation generate verus --crate-path <root>
//
// No imports from the VSM crate — self-contained for vargo builds.
// Architecture:
//   V11/V12 leaf + composition proof grounded by V13 assume_specification.
//   Each stub is trusted axiomatically (requires/ensures mirrors the
//   formal_method contract); Kani and Creusot independently verify the
//   real transition bodies in elicit_server.

use verus_builtin_macros::verus;
use vstd::prelude::*;

// ─── External transition stubs ─────────────────────────────────────────────────
// These functions represent the real transitions in elicit_server.
// Verus does not verify their bodies; assume_specification below injects
// the trusted contracts.  Kani/Creusot independently verify the real bodies.

/// Stub for `initialize_combat` — body is opaque to Verus.
#[verifier::external]
pub fn initialize_combat_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `begin_turn` — body is opaque to Verus.
#[verifier::external]
pub fn begin_turn_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `declare_attack` — body is opaque to Verus.
#[verifier::external]
pub fn declare_attack_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `resolve_attack` — body is opaque to Verus.
#[verifier::external]
pub fn resolve_attack_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `resolve_defense` — body is opaque to Verus.
#[verifier::external]
pub fn resolve_defense_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `apply_damage` — body is opaque to Verus.
#[verifier::external]
pub fn apply_damage_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `complete_movement_action` — body is opaque to Verus.
#[verifier::external]
pub fn complete_movement_action_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `end_turn` — body is opaque to Verus.
#[verifier::external]
pub fn end_turn_stub(state: CombatState) -> CombatState {
    todo!()
}

/// Stub for `conclude_combat` — body is opaque to Verus.
#[verifier::external]
pub fn conclude_combat_stub(state: CombatState) -> CombatState {
    todo!()
}

verus! {

/// Abstract mirror of `CombatState` (invariant-relevant variants only).
#[allow(unused_imports)]
use vstd::prelude::SpecOrd;

#[derive(Debug, PartialEq, Eq)]
pub enum CombatState {
    _Unspecified,
}

/// Invariant for `CombatMachine`.
pub open spec fn CombatConsistent(state: &CombatState) -> bool { true }

// ─── Postcondition predicates ──────────────────────────────────────────────────

/// Any non-special post-state — the `_ => true` arm of the invariant applies.
pub open spec fn combat_post_trivial(post: CombatState) -> bool { true }

/// Passthrough: post-state equals pre-state.
pub open spec fn combat_post_passthrough(pre: CombatState, post: CombatState) -> bool { post == pre }

// ─── Leaf lemmas ────────────────────────────────────────────────────────────────

/// Trivial post satisfies the invariant (`_ => true` arm applies).
pub proof fn combat_leaf_trivial(post: CombatState)
    requires combat_post_trivial(post),
    ensures  CombatConsistent(&post),
{}

/// Passthrough preserves the invariant by induction.
pub proof fn combat_leaf_passthrough(pre: CombatState, post: CombatState)
    requires
        CombatConsistent(&pre),
        combat_post_passthrough(pre, post),
    ensures CombatConsistent(&post),
{}

// ─── Assume specifications (V13 trust anchors) ──────────────────────────────────

// Each assume_specification axiomatizes the contract for its stub.
// The postcondition predicate is the same one used by the leaf lemma above,
// ensuring Verus proof and assume_specification are tightly aligned.

/// Contract for `initialize_combat_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `initialize_combat` body.
pub assume_specification[initialize_combat_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_trivial(r);

/// Contract for `begin_turn_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `begin_turn` body.
pub assume_specification[begin_turn_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_trivial(r);

/// Contract for `declare_attack_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `declare_attack` body.
pub assume_specification[declare_attack_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_trivial(r);

/// Contract for `resolve_attack_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `resolve_attack` body.
pub assume_specification[resolve_attack_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_trivial(r);

/// Contract for `resolve_defense_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `resolve_defense` body.
pub assume_specification[resolve_defense_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_trivial(r);

/// Contract for `apply_damage_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `apply_damage` body.
pub assume_specification[apply_damage_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_passthrough(state, r);

/// Contract for `complete_movement_action_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `complete_movement_action` body.
pub assume_specification[complete_movement_action_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_trivial(r);

/// Contract for `end_turn_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `end_turn` body.
pub assume_specification[end_turn_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_passthrough(state, r);

/// Contract for `conclude_combat_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `conclude_combat` body.
pub assume_specification[conclude_combat_stub](state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  combat_post_trivial(r);

// ─── Verified exec callers ───────────────────────────────────────────────────────

// These exec fns close the proof loop: calling any stub on a consistent
// pre-state yields a consistent post-state, by the assume_specification.

/// Proof that `initialize_combat` preserves the invariant (via assume_specification).
pub fn initialize_combat_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    initialize_combat_stub(state)
}

/// Proof that `begin_turn` preserves the invariant (via assume_specification).
pub fn begin_turn_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    begin_turn_stub(state)
}

/// Proof that `declare_attack` preserves the invariant (via assume_specification).
pub fn declare_attack_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    declare_attack_stub(state)
}

/// Proof that `resolve_attack` preserves the invariant (via assume_specification).
pub fn resolve_attack_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    resolve_attack_stub(state)
}

/// Proof that `resolve_defense` preserves the invariant (via assume_specification).
pub fn resolve_defense_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    resolve_defense_stub(state)
}

/// Proof that `apply_damage` preserves the invariant (via assume_specification).
pub fn apply_damage_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    apply_damage_stub(state)
}

/// Proof that `complete_movement_action` preserves the invariant (via assume_specification).
pub fn complete_movement_action_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    complete_movement_action_stub(state)
}

/// Proof that `end_turn` preserves the invariant (via assume_specification).
pub fn end_turn_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    end_turn_stub(state)
}

/// Proof that `conclude_combat` preserves the invariant (via assume_specification).
pub fn conclude_combat_verified(state: CombatState) -> (r: CombatState)
    requires CombatConsistent(&state),
    ensures  CombatConsistent(&r),
{
    conclude_combat_stub(state)
}

// ─── Transition tags + composition ──────────────────────────────────────────────

/// One tag per `CombatMachine` transition — dispatch without re-verifying bodies.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum CombatMachineTrans {
    InitializeCombat,
    BeginTurn,
    DeclareAttack,
    ResolveAttack,
    ResolveDefense,
    ApplyDamage,
    CompleteMovementAction,
    EndTurn,
    ConcludeCombat,
}

/// Maps each transition tag to its postcondition predicate.
pub open spec fn combat_post(pre: CombatState, post: CombatState, tag: CombatMachineTrans) -> bool {
    match tag {
        CombatMachineTrans::InitializeCombat => combat_post_trivial(post),
        CombatMachineTrans::BeginTurn => combat_post_trivial(post),
        CombatMachineTrans::DeclareAttack => combat_post_trivial(post),
        CombatMachineTrans::ResolveAttack => combat_post_trivial(post),
        CombatMachineTrans::ResolveDefense => combat_post_trivial(post),
        CombatMachineTrans::ApplyDamage => combat_post_passthrough(pre, post),
        CombatMachineTrans::CompleteMovementAction => combat_post_trivial(post),
        CombatMachineTrans::EndTurn => combat_post_passthrough(pre, post),
        CombatMachineTrans::ConcludeCombat => combat_post_trivial(post),
    }
}

/// Composition: any post-state produced by a tagged transition satisfies the invariant.
pub proof fn combat_composition(pre: CombatState, post: CombatState, tag: CombatMachineTrans)
    requires
        CombatConsistent(&pre),
        combat_post(pre, post, tag),
    ensures CombatConsistent(&post),
{
    match tag {
        CombatMachineTrans::InitializeCombat => combat_leaf_trivial(post),
        CombatMachineTrans::BeginTurn => combat_leaf_trivial(post),
        CombatMachineTrans::DeclareAttack => combat_leaf_trivial(post),
        CombatMachineTrans::ResolveAttack => combat_leaf_trivial(post),
        CombatMachineTrans::ResolveDefense => combat_leaf_trivial(post),
        CombatMachineTrans::ApplyDamage => combat_leaf_passthrough(pre, post),
        CombatMachineTrans::CompleteMovementAction => combat_leaf_trivial(post),
        CombatMachineTrans::EndTurn => combat_leaf_passthrough(pre, post),
        CombatMachineTrans::ConcludeCombat => combat_leaf_trivial(post),
    }
}

} // verus!
